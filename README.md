# std / android_annotation_bind_value
Bind Value From Bundle (Activity->Activity / Activity->Fragment)

@GetValue("extra-key", "type_value")
val value: Type

/**
* StdBindValue.bindValue(<Class>, bundle)
*/
